path:
  data: /data/es-data
  logs: /data/es-log
  plugins: /usr/share/elasticsearch/plugins
  work: /data/es-work
network:
  host: 0.0.0.0
discovery:
  zen:
    ping:
      multicast:
        enabled: true
cluster:
  name: ${CLUSTER_NAME}